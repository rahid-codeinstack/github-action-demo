name: Deploy Vanilla JS App

on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v3
      - name: checkout action 
        uses: action/checkout@v3


      